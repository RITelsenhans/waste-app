spring:
  datasource:
    url: jdbc:h2:mem:waste;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    driver-class-name: org.h2.Driver
  flyway:
    locations: classpath:db/migration

waste:
  pilot:
    admin-enabled: true
